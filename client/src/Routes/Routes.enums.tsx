enum RoutesValues {
  root = "/",
  signIn = "/sign-in",
  signUp = "/sign-up",
  monthTraverse = "/month-traverse",
  currentMonth = "/current-month",
  date = `day/:date`,
  notFound = "/404",
}

export default RoutesValues;
